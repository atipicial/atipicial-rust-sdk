global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    monitor: 'atipicialrust-monitor'
    environment: 'development'

# Alert manager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093

# Load rules once and periodically evaluate them
rule_files:
  - "alerts.yml"

scrape_configs:
  # AtipicialRust SDK metrics
  - job_name: 'atipicialrust-sdk'
    static_configs:
      - targets: ['host.docker.internal:9090']
        labels:
          component: 'sdk'
          
  # Atipicial CLI metrics
  - job_name: 'atipicial-cli'
    static_configs:
      - targets: ['host.docker.internal:9091']
        labels:
          component: 'cli'
          
  # Node Exporter (system metrics)
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
        labels:
          component: 'system'
          
  # OpenTelemetry Collector metrics
  - job_name: 'otel-collector'
    static_configs:
      - targets: ['otel-collector:8888']
        labels:
          component: 'collector'
          
  # Prometheus self-monitoring
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
        labels:
          component: 'prometheus'
